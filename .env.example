NEXT_PUBLIC_APP_NAME=JoJobuddy
MONGODB_URI=mongodb://127.0.0.1:27017/jojobuddy
# Optional. Default 20. Use a smaller value for worker processes (compose uses 5).
# MONGODB_MAX_POOL_SIZE=20

# Auth (use a long random string in production)
AUTH_SECRET=change-me-to-a-long-random-string
AUTH_URL=http://localhost:3000

# Google OAuth: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: {AUTH_URL}/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
